<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />

    <!-- JALANKAN FILE CSS INI KETIKA HALAMAN NYA TIDAK JELAS (START) -->
    <% if (!css) { %>
    <link rel="stylesheet" href="stylesheets/default.css" />
    <% } %>
    <!-- JALANKAN FILE CSS INI KETIKA HALAMAN NYA TIDAK JELAS (START) -->

    <!-- JALANKAN FILE CSS INI KETIKA KETEMU HALAMAN YG DITUJU (START) -->
    <% if (css) { %>
    <link rel="stylesheet" href="stylesheets/navbar.css" />
    <% } %>

    <!-- CSS UNTUK HOME -->
    <% if (css === 'home') { %>
    <link rel="stylesheet" href="stylesheets/home.css" />
    <% } %>

    <!-- CSS UNTUK DASHBOARD -->
    <% if (css === 'dashboard') { %>
    <link rel="stylesheet" href="stylesheets/dashboard.css" />
    <% } %>

    <!-- CSS UNTUK ABOUT -->
    <% if (css === 'about') { %>
    <link rel="stylesheet" href="stylesheets/ourteam.css" />
    <% } %>
    <!-- JALANKAN FILE CSS INI KETIKA KETEMU HALAMAN YG DITUJU (END) -->
  </head>
  <body>
    <div class="container-fluid" style="--bs-gutter-x: 0 !important">
      <!-- NAVBAR START -->
      <% if (html) { %> 
        <nav class="navbar navbar-expand-lg w-100" id="navbarContainer">
          <div class="d-flex justify-content-between w-100">
            <div class="collapse navbar-collapse" id="icons">
              <ul class="navbar-nav">
                <li class="nav-item">
                  <img
                    src="./icons/plant-guard-logo.png"
                    alt=""
                    width="70"
                    height="70"
                  />
                </li>
              </ul>
            </div>
            <div class="collapse navbar-collapse" id="navbarNav">
              <!-- PR : UBAH CODE INI JADI LEBIH RINGKAS -->
              <ul class="navbar-nav" id="">
                <li class="nav-item" id="home">
                  <a
                    class="nav-link active"
                    href="http://localhost:3000/home"
                    value="home"
                  >
                    <img
                      src="./icons/green-bullet.png"
                      width="15"
                      height="15"
                      alt=""
                    />
                    Home
                  </a>
                </li>
                <li class="nav-item" id="dashboard">
                  <a class="nav-link" href="http://localhost:3000/dashboard">
                    <img
                      src="./icons/green-bullet.png"
                      width="15"
                      height="15"
                      style="visibility: hidden"
                      alt=""
                    />
                    Dashboard
                  </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="http://localhost:3000/ourteam"
                    >About</a
                  >
                </li>
              </ul>
            </div>
          </div>
        </nav>
      <% } %>
      <!-- NAVBAR END -->

      <!-- HALAMAN NOT FOUND START -->
      <% if (!html) { %> <%- body %> <% } %>
      <!-- HALAMAN NOT FOUND END -->

      <!-- HALAMAN HOME START -->
      <% if (html === 'home') { %> <%- body %> <% } %>
      <!-- HALAMAN HOME END -->

      <!-- HALAMAN DASHBOARD START -->
      <% if (html === 'dashboard') { %> <%- body %> <% } %>
      <!-- HALAMAN DASHBOARD END -->

      <!-- HALAMAN ABOUT START -->
      <% if (html === 'about') { %>
      <!-- BODY DI SINI TERHUBUNG DENGAN LAYOUT PADA ROUTES -->
      <%- body %> <% } %>
      <!-- HALAMAN ABOUT END -->
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
